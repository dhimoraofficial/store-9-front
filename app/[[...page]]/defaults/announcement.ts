import { ComponentSchema } from "@/application/runtime/builder/type";

/**
 * Default Announcement Bar Schema
 * Can be populated separately. Note: The announcement bar strip is currently integrated 
 * within the navbarSchema for layout consistency, so this defaults to empty.
 */
export const defaultAnnouncementBar: ComponentSchema[] = [];
